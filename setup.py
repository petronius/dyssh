#!/usr/bin/env python

from setuptools import setup

setup(
    name = 'dyssh',
    version = "0.0.1",
    description = (''),
    author = 'Michael C Schuller',
    packages = ['dyssh'],
    classifiers = [],
)
